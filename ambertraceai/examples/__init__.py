"""Bundled SDK example scripts.

This sub-package is populated at build time (``scripts/generate_sdk.sh`` copies
the runnable examples from the top-level ``examples/`` directory into
``ambertraceai/examples/`` so they ship inside the wheel). The discovery surface
is :func:`ambertraceai.sdk_examples` -- use that, not direct imports from here.
"""
